#installing ufw tool
sudo apt-get update
sudo apt-get install ufw

#denying all ports
sudo ufw default deny incoming

#allowing the required ports
sudo ufw allow 22/tcp
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp

#enabling the firewall
sudo ufw enable
